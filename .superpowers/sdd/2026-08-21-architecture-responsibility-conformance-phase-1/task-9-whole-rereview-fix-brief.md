# Task 9 whole-rereview fix wave

## Baseline

- Exact clean start: `9f467ad09f5d3cf9a182cce2d6e8dc1edc2ad969`.
- Task 9 BASE: `0cbb95afb835263ecf3b52b36f5ec0605f903435`; retain one amended commit.
- Verify each finding, strict focused RED -> GREEN, one implementer/no subagents.
- No default/full suite; no overlap or edits during active suites; no Phase 2/main/issue.

## Critical

1. Executable review artifact must contain and derive every canonical architecture gate: Rule Resolution, Canonical Selector, Production Activation-path, behavior state, Change Hygiene, Architecture/Tree/Responsibility/Verification verdicts, overall verdict, Critical/Major counts. Handoff must agree with all visible derived verdicts.
2. Replace language-blind comment/executable classification. Valid spaced C/JS statements and Rust/C# directives outside owner coverage must be executable or fail-closed. Preserve true comments using path/language-aware or safely ambiguous semantics.
3. Terminal Task Provenance `task_unit` must be selected migration-unit ID for migration-unit adapters and Work Item ID otherwise, derived from approved selector/adapter authority in reconciliation and completion paths.
4. Structural-gate and flexible-scope authority-bearing Markdown parsing must share semantic visible Markdown rules; fenced/commented/indented examples cannot inflate headings/tables, canonical visible content passes.
5. A wholly commented-out production owner/route must not create an active owner or satisfy inventory/review.
6. A wholly commented-out verification scenario/subject/provider/binding must not satisfy review evidence.
7. JavaScript regex literals must not corrupt brace/block ownership tracking; regex braces are inert and later rogue code remains unowned.
8. Pinned task-base must be an ancestor of final HEAD; unrelated reachable commits reject before inventory acceptance.
9. SourceIntegrity must reject top-level/named-block registrations dominated by preceding unconditional terminal statements, not only nested foreach blocks.

## Important

10. Enforce Change Hygiene semantics for A/M/C/R/D: canonical Edited Region, formatter command, unrelated-diff disposition, and one-to-one review table/Major findings; repository-wide formatter or confirmed unrelated diff cannot silently pass.
11. Review handoff cells must exactly agree with derived visible verdicts; contradictory BLOCKED handoff cannot be producer-valid.
12. Copy classification is destination-based: production->excluded copy must not require production ownership for excluded destination, while rename preserves old/new movement authority.
13. Canonical Git paths must safely support spaces and Unicode, use NUL-delimited name-status parsing, reject traversal/aliases/control chars; normalize trailing root separator (deferred Minor mechanically resolved).
14. Define language-valid source marker encoding for real producers and consumers. Contract/skill/template must specify comment-wrapped marker forms appropriate to languages; parser consumes semantic active metadata without requiring invalid bare pseudo-statements. Add composed valid Dart/Java/Python/C#/Rust positives and commented-out/inert negatives.

## Verification

- Focused real Git/PS5.1 tests for every item.
- Affected suites, corrected13 short gates, focused17, AST/diff/scope audit.
- Append report/ledger, stage approved files, amend sole commit, exact-HEAD focused/All/SourceIntegrity/diff/status/count.
- Return DONE/SHA/tests/concerns. Full remains controller-owned.
